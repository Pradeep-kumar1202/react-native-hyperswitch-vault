/*
 * Type entry for `<package>/vault`. Every type below is generated from ReScript by genType in
 * `VaultConfirm.gen.tsx`; this file only maps the public names.
 */
import './jsx-global';

export { confirmPaymentMethodSession } from './VaultConfirm.gen';

export type {
  vaultEnvironment,
  cardDetails,
  confirmRequest,
  vaultCardMetadata,
  vaultConfirmResult,
  vaultErrorCode,
  vaultError,
  confirmOutcome,
} from './VaultConfirm.gen';
